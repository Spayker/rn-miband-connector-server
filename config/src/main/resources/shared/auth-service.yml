spring:
  data:
    mongodb:
      host: auth-mongodb
      username: user
      password: ${MONGODB_PASSWORD}
      database: mservicet
      port: 27017

server:
  servlet:
    context-path: /mservicet
  port: 5000
